# 📚 Каталог курсов — Vue 3 + WordPress

Компонент для отображения каталога онлайн-курсов с фильтрацией, поиском и сортировкой.

## 🚀 Установка в WordPress

### 1. Загрузка файлов

Скопируй папку `courses-app` в тему WordPress:

```
/wp-content/themes/flavor/courses-app/
├── courses.js          # Основной скрипт
├── courses.css         # Стили
├── data/
│   └── courses.json    # Данные курсов
└── img/
    └── placeholder.png # Заглушка для обложек (опционально)
```

### 2. Подключение скриптов

В файл `functions.php` твоей темы добавь:

```php
// Каталог курсов
function enqueue_courses_app() {
    // Только на нужной странице (измени slug)
    if (is_page('kursy') || is_page('courses')) {
        
        // Vue 3
        wp_enqueue_script(
            'vue-3',
            'https://unpkg.com/vue@3/dist/vue.global.prod.js',
            array(),
            '3.4',
            true
        );
        
        // Стили каталога
        wp_enqueue_style(
            'courses-app-css',
            get_template_directory_uri() . '/courses-app/courses.css',
            array(),
            '1.0'
        );
        
        // Скрипт каталога
        wp_enqueue_script(
            'courses-app-js',
            get_template_directory_uri() . '/courses-app/courses.js',
            array('vue-3'),
            '1.0',
            true
        );
    }
}
add_action('wp_enqueue_scripts', 'enqueue_courses_app');
```

### 3. Вставка в Elementor

1. Открой страницу в Elementor
2. Добавь виджет **HTML**
3. Вставь код:

```html
<div id="courses-app"></div>
```

4. Сохрани и опубликуй

---

## 📁 Структура данных курса

```json
{
  "id": "123",
  "name": "Python-разработчик",
  "tariff": "расширенный курс",
  
  "school": "Нетология",
  "school_alias": "netologiya",
  "school_logo": "https://...",
  "school_rating": 4.9,
  "school_reviews_count": 1537,
  
  "cover_url": "https://...",
  "course_url": "https://...",
  
  "price": 126000,
  "price_original": 210000,
  "price_installment": 3500,
  
  "duration_months": 14,
  
  "learning_type": ["typeProgramming"],
  "directions": ["Python", "Backend", "Django"],
  "levels": ["Начинающий"],
  "formats": ["Записи", "Онлайн"],
  
  "is_top_sale": true,
  "is_wow_effect": false,
  "job_help": true,
  
  "diploma": true,
  "mentor": true
}
```

---

## ⚙️ Настройка

### Путь к JSON

В файле `courses.js` измени путь к данным (строка ~290):

```javascript
const response = await fetch('/wp-content/themes/flavor/courses-app/data/courses.json');
```

### Цвета (CSS переменные)

В `courses.css` в начале файла:

```css
:root {
  --bg-primary: #0f0f1a;        /* Фон страницы */
  --bg-card: #1e1e32;           /* Фон карточки */
  --accent-primary: #00d4ff;    /* Акцентный цвет */
  --text-primary: #ffffff;       /* Основной текст */
}
```

### Количество карточек

В `courses.js`:

```javascript
const perPage = ref(12);  // Карточек на странице
```

---

## 🎨 Фильтры

### Быстрые фильтры (теги)
- Направления (Python, JavaScript, React...)
- Уровни (С нуля, С опытом)
- С трудоустройством
- Топ школы

### Выпадающие фильтры
- Стоимость (диапазоны)
- Срок обучения
- Школа

### Модальное окно "Все фильтры"
- Уровень
- Категория
- Цель обучения
- Стоимость
- Срок
- Дополнительно (ВАУ курсы, рассрочка...)

---

## 📱 Адаптивность

- **Desktop:** 4 колонки
- **Tablet (< 1200px):** 3 колонки
- **Tablet (< 900px):** 2 колонки
- **Mobile (< 600px):** 1 колонка

Фильтры на мобильных — горизонтальный скролл.

---

## 🔄 Обновление данных

Для обновления курсов:

1. Запусти парсер:
```bash
python courses_parser.py
```

2. Скопируй JSON в WordPress:
```bash
cp all_courses_combined.json /wp-content/themes/flavor/courses-app/data/courses.json
```

Или настрой автоматическую синхронизацию через cron.

---

## 📊 Производительность

- **4500 курсов** — работает мгновенно
- Фильтрация на клиенте (без запросов к серверу)
- Ленивая загрузка изображений (`loading="lazy"`)
- Минимальный бандл (Vue ~33kb gzip)

---

## 🛠 Возможные доработки

1. **Слайдер цены** — вместо кнопок-диапазонов
2. **Сравнение курсов** — добавить в избранное
3. **URL-параметры** — сохранять фильтры в URL
4. **SEO** — серверный рендеринг для индексации

---

## 📞 Поддержка

Если нужны доработки — пиши!
